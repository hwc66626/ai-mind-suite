"""Brain Memory MCP —— 模拟人脑记忆机制的记忆引擎。

核心机制（科学依据见 README）：
- 双强度模型：存储强度（硬盘）/ 提取强度（内存），Bjork 新失用理论
- 艾宾浩斯遗忘曲线 R(t) = e^(-t/S) 与间隔效应（合意困难）
- 冷热分层：hot（工作记忆/RAM）/ warm（近期长期）/ cold（深度归档，可唤醒）
- 局部权重：同一记忆在不同分类（图式）内享有独立权重
- 长期目标全局加权：价值导向记忆（VDR）
- 情绪加权：唤醒度放大编码强度与衰减时间常数（杏仁核机制）
- 扩散激活：联想网络 A(j) = Σ w_ij · A(i) · γ^hop（Collins & Loftus）
- 睡眠固化：去重合并、情景→语义压缩、目标重放、冷归档
- 软纠错：只标记/降权，永不物理删除，可翻案恢复
"""

__version__ = "0.1.0"
